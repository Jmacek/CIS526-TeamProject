extends layout
block content
    .site-wrapper(style='background-size: cover')
        .site-wrapper-inner
            .cover-container.row.row-centered
                .inner.cover.col-centered.col-max
                    form.form-signin(action='/register', method='post')
                        input(type='hidden', name='${_csrf.parameterName}', value='${_csrf.token}')
                        h2.form-signin-heading Register for an account here
                        label.sr-only(for='inputEmail') Username
                        input#inputEmail.form-control(type='text', placeholder='Username', name='username', required='', autofocus='')
                        label.sr-only(for='inputPassword') Password
                        input#inputPassword.form-control(type='password', placeholder='Password', name='password', required='')
                        button.btn.btn-lg.btn-primary.btn-block(type='submit') Sign up
                    c:if(test='${param.register ne null}')
                        .alert.alert-success(role='alert')
                            strong Success!
                            |  You have created an account.
                    c:if(test='${param.error ne null}')
//TODO: When userauth is up and running, display appropriate messages